This component exists in order to render the LostItems React component on the
client side. This is what the client:only attribute achieves, which is only
available within .astro and .mdx files.
---
import LostItemForm from '@/components/LostItemForm'
---
<LostItemForm client:only="react" />
